# App Evaluation Report — 2026-07-01
Generated by /evaluate-app skill.
Input sources: pyproject.toml, agent_config.yaml, config.yaml, server.py, Makefile, docker-compose.yml, .env.example, src/server/app/ (all routers), src/server/auth/, src/server/database/, src/server/services/, src/server/models/, src/server/handlers/, src/server/dependencies/, src/server/utils/, src/ptc_agent/agent/agent.py, src/ptc_agent/agent/middleware/ (29 middleware files), src/config/settings.py, src/observability/, src/tools/, src/utils/cache/, src/llms/, mcp_servers/, skills/, migrations/versions/, deploy/, tests/unit/, tests/integration/, web/src/pages/, web/src/components/, web/src/hooks/, web/src/api/, web/src/contexts/, web/e2e/, CLAUDE.md, src/server/CLAUDE.md

## Executive Summary

LangAlpha is a well-architected financial AI research platform with strong fundamentals: clean middleware-driven agent composition, comprehensive OTel observability, SHA-256 data provenance, 566+ test files, and a rich feature set spanning 25 agent skills, configurable dashboard widgets, and automated market insights. However, **three critical issues demand immediate attention**: a CORS wildcard-with-credentials configuration that enables cross-origin account takeover, a hardcoded default encryption key protecting all user API keys and OAuth tokens, and a single-worker uvicorn deployment that will collapse under moderate concurrent load. The highest-value first action is fixing the CORS and encryption key issues — both are low-effort, high-impact security fixes for a financial platform.

## Evaluation Scope
| Domain                              | Files Reviewed | Findings |
|-------------------------------------|----------------|----------|
| Architecture & Code Quality         | ~45            | 14       |
| Feature Gaps & Enhancements         | ~55            | 31       |
| Security & Compliance               | ~50            | 23       |
| Performance & Scalability           | ~40            | 20       |

## Ranked Enhancement List

### Tier 1 — Do First (Score >= 6)
| # | Finding | Domain | Severity | Effort | Impact | Score |
|---|---------|--------|----------|--------|--------|-------|
| 1 | CORS wildcard `*` with `allow_credentials=True` enables cross-origin account takeover | Security | Critical | Low | High | 6 |
| 2 | Default encryption key (`langalpha-local-dev-encryption-key`) hardcoded in docker-compose — all API keys/OAuth tokens compromised if unset | Security | Critical | Low | High | 6 |
| 3 | Single uvicorn worker — all traffic on one event loop, CPU-bound middleware stalls everything | Performance | Critical | Low | High | 6 |

### Tier 2 — Do Next (Score 4-5)
| # | Finding | Domain | Severity | Effort | Impact | Score |
|---|---------|--------|----------|--------|--------|-------|
| 4 | Exception details leaked to HTTP clients via `str(e)` in 45+ call sites | Architecture | High | Low | High | 5 |
| 5 | `calculate_next_thread_index` silently returns 0 on DB error, masking data integrity issues | Architecture | High | Low | High | 5 |
| 6 | Default DB credentials (postgres/postgres) in docker-compose and connection code | Security | High | Low | High | 5 |
| 7 | SSL disabled for non-Supabase database connections — credentials in plaintext | Security | High | Low | High | 5 |
| 8 | In-process singleton architecture prevents multi-worker horizontal scaling | Performance | Critical | High | High | 4 |
| 9 | PostgreSQL connection pool exhaustion risk (50+25 per worker vs default max_connections=100) | Performance | High | Medium | High | 4 |
| 10 | Redis connection pool (150 max) under heavy SSE streaming pressure | Performance | Medium | Low | High | 4 |
| 11 | Docker socket mounted into backend container — root-equivalent host access | Security | High | Medium | High | 4 |
| 12 | OAuth email (PII) logged at info level | Security | High | Low | Medium | 4 |
| 13 | Thread hard-delete with CASCADE destroys financial conversation records permanently | Security | High | Medium | High | 4 |
| 14 | No notification/alert delivery system beyond webhooks (no email, push, Slack) | Features | High | Medium | High | 4 |
| 15 | No automated report scheduling with email delivery — unlocks existing automation value | Features | High | Medium | High | 4 |
| 16 | `get_user_stats()` performs unbounded 4-table JOIN aggregation — full table scan | Performance | High | Medium | High | 4 |
| 17 | SSE connection fan-out: 2-4 long-lived connections per user, FD limit ceiling at ~250-500 users | Performance | High | Medium | High | 4 |

### Tier 3 — Do Later (Score <= 3)
| # | Finding | Domain | Severity | Effort | Impact | Score |
|---|---------|--------|----------|--------|--------|-------|
| 18 | No unit tests for `_handle_send_message` — the 350-line critical chat hot path | Architecture | High | High | High | 3 |
| 19 | Credit/usage functions embedded in 2,734-line conversation.py — financial audit boundary concern | Architecture | Medium | Medium | High | 3 |
| 20 | Single symmetric encryption key for all users — compromise exposes everything | Security | High | High | High | 3 |
| 21 | No formal audit trail for financial actions (portfolio CRUD, watchlist changes, thread deletions) | Security | High | High | High | 3 |
| 22 | No data retention policy — no TTL, archival, or scheduled PII cleanup | Security | High | High | High | 3 |
| 23 | PII (email, name, avatar_url) stored unencrypted in users table | Security | High | High | High | 3 |
| 24 | Portfolio financial positions (quantity, cost basis, account names) stored unencrypted | Security | High | High | High | 3 |
| 25 | MCP subprocess explosion — 10 stdio subprocesses per workspace, linear with concurrent users | Performance | High | High | High | 3 |
| 26 | No portfolio P&L tracking or performance visualization page | Features | High | High | High | 3 |
| 27 | No admin dashboard or admin API endpoints | Features | High | High | High | 3 |
| 28 | No Grafana/Prometheus dashboards despite comprehensive OTel instrumentation | Features | Medium | Medium | High | 3 |
| 29 | No structured centralized log shipping (stdout only) | Features | Medium | Low | Medium | 3 |
| 30 | No Slack/Discord delivery integration for automations | Features | Medium | Medium | High | 3 |
| 31 | Auth bypass in OSS mode with static user ID (by design but risky if deployed to production) | Security | Medium | Low | Medium | 3 |
| 32 | WebSocket JWT token accepted via query parameter, exposed in URL/logs | Security | Medium | Low | Medium | 3 |
| 33 | Sessions endpoint lacks authentication — exposes active session stats | Security | Medium | Low | Medium | 3 |
| 34 | Service-to-service auth uses single shared secret with user impersonation via X-User-Id | Security | Medium | Medium | High | 3 |
| 35 | ChatRequest messages content field has no max_length constraint | Security | Medium | Low | Medium | 3 |
| 36 | SSRF protection only active in platform mode — OSS mode unprotected | Security | Medium | Low | Medium | 3 |
| 37 | Backend container runs as root (no USER directive in Dockerfiles) | Security | Medium | Low | Medium | 3 |
| 38 | Frontend/backend ports not bound to 127.0.0.1 in docker-compose | Security | Medium | Low | Medium | 3 |
| 39 | API key masking reveals first 3 + last 4 chars — brute-forceable for short keys | Security | Medium | Low | Medium | 3 |
| 40 | `get_workspace_stats()` pulls all JSONB blobs into Python for aggregation instead of SQL | Performance | Medium | Low | Medium | 3 |
| 41 | Missing standalone index on `conversation_threads(workspace_id)` | Performance | Medium | Low | Medium | 3 |
| 42 | Large JSONB `sse_events` blobs fetched unnecessarily in some query paths | Performance | Medium | Medium | High | 3 |
| 43 | Unbounded `get_responses_for_thread()` and `get_queries_for_thread()` — no default limit | Performance | Medium | Low | Medium | 3 |
| 44 | LangGraph checkpoint pool (25 max) can exhaust during long-running workflows | Performance | Medium | Medium | High | 3 |
| 45 | Middleware token counting (tiktoken) is O(n) in message length on every model call | Performance | Medium | Low | Medium | 3 |
| 46 | Frontend API client has no timeout or retry configuration | Performance | Medium | Low | Medium | 3 |
| 47 | Daytona sandbox cold-start latency under burst creation | Performance | Medium | Low | Medium | 3 |
| 48 | Duplicated PTC-dispatch vs flash-dispatch admission logic in threads.py | Architecture | Medium | Medium | Medium | 2 |
| 49 | Duplicated `if conn:` branching across 15 database functions | Architecture | Medium | Medium | Medium | 2 |
| 50 | Temp diagnostic middleware (`MalformedIdDiagnosticMiddleware`) left in production | Architecture | Medium | Low | Low | 2 |
| 51 | SSE event accumulation uses `copy.deepcopy()` on every event — CPU overhead | Performance | Medium | Medium | Medium | 2 |
| 52 | No user/workspace data caching despite being fetched on nearly every request | Performance | Medium | Medium | Medium | 2 |
| 53 | No rate limiting on most API endpoints (only chat messages and API key testing) | Security | Medium | Medium | Medium | 2 |
| 54 | Error responses leak internal exception details (duplicate of #4 from security lens) | Security | Medium | Medium | Medium | 2 |
| 55 | Watchlist `alert_settings` stored but never acted upon | Features | Medium | Medium | Medium | 2 |
| 56 | No conversation search or global search across threads | Features | Medium | Medium | Medium | 2 |
| 57 | No keyboard shortcuts for power-user navigation | Features | Medium | Medium | Medium | 2 |
| 58 | No broker integration for live positions/trade execution | Features | Medium | High | High | 2 |
| 59 | No migration tests or rollback validation | Features | Medium | Medium | Medium | 2 |
| 60 | E2E tests cover only 3/7+ page routes | Features | Medium | Medium | Medium | 2 |
| 61 | Mobile layout gaps on MarketView and Automations pages | Features | Medium | Medium | Medium | 2 |
| 62 | No MCP server health monitoring or data staleness alerts | Features | Medium | Medium | Medium | 2 |
| 63 | No portfolio rebalancing alerts | Features | Medium | Medium | Medium | 2 |
| 64 | No API documentation hosting beyond auto-generated /docs | Features | Low | Low | Medium | 2 |
| 65 | No skeleton/shimmer loading states | Features | Low | Low | Medium | 2 |
| 66 | No calendar export (iCal/Google Calendar) for earnings/events | Features | Low | Low | Medium | 2 |
| 67 | No bundle size analysis or frontend performance monitoring | Features | Low | Low | Medium | 2 |
| 68 | No watchlist pagination — fetches all items unbounded | Performance | Low | Low | Low | 1 |
| 69 | Redis `delete_pattern()` not pipelined — individual DELETEs in a SCAN loop | Performance | Low | Low | Low | 1 |
| 70 | `get_next_turn_index()` uses COUNT(*) instead of MAX()+1 | Performance | Low | Low | Low | 1 |
| 71 | Module globals as DI container in setup.py | Architecture | Medium | High | Medium | 1 |
| 72 | conversation.py 2,734-line monolith (beyond credit/usage concern) | Architecture | Medium | High | Medium | 1 |
| 73 | INTERNAL_SERVER_ERROR_DETAIL defined in 3 files, never used | Architecture | Low | Low | Low | 1 |
| 74 | WorkspaceThreadListItem construction duplication | Architecture | Low | Low | Low | 1 |
| 75 | Legacy stub section in conversation.py | Architecture | Low | Low | Low | 1 |
| 76 | server.py defaults to 0.0.0.0 binding | Architecture | Low | Low | Low | 1 |
| 77 | No conversation export (PDF/DOCX) from UI | Features | Low | Medium | Medium | 1 |
| 78 | Only 2 locales (en-US, zh-CN) | Features | Low | Medium | Medium | 1 |
| 79 | No multi-user collaboration on workspaces | Features | Medium | High | Medium | 1 |
| 80 | No stock screener/scanner UI | Features | Medium | High | Medium | 1 |
| 81 | No backtesting capability | Features | Medium | High | Medium | 1 |
| 82 | No Bloomberg/Refinitiv/FactSet integration | Features | Medium | High | Medium | 1 |
| 83 | No CI performance regression detection | Features | Low | Medium | Medium | 1 |
| 84 | No dark/light theme preview in settings | Features | Low | Low | Low | 1 |
| 85 | No drag-and-drop reordering for watchlists | Features | Low | Low | Low | 1 |
| 86 | Mixed logging libraries (logging vs structlog) | Architecture | Low | Medium | Low | 0 |
| 87 | No Storybook or visual component catalog | Features | Low | Medium | Low | 0 |
| 88 | No offline/degraded mode handling | Features | Low | Medium | Low | 0 |

## Domain Detail

### Architecture & Code Quality

**Strengths:**
- Clean layered architecture with clear separation: `src/server/` (FastAPI), `src/ptc_agent/` (agent library), `src/tools/` (LangChain tools), `web/src/` (React frontend)
- Middleware-driven agent composition (~29 middleware classes), each with focused single responsibility
- Excellent documentation: CLAUDE.md files at project and module levels, thorough "why" comments
- Strong test suite: 315 unit test files, 68 integration files, 183 frontend test files
- Data provenance system with SHA-256 content addressing and integrity verification
- Robust startup/shutdown lifecycle with 11-step orderly teardown and graceful degradation
- Low tech debt surface: only 24 TODO/FIXME/HACK across 384 Python source files
- Consistent naming conventions: snake_case for Python, camelCase for JS, PascalCase for classes

**Finding: Exception details leaked to HTTP clients via `str(e)` in 45+ call sites**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/app/threads.py` lines 305, 362, 396, 818, 989. Pattern: `detail=f"Failed to list threads: {str(e)}"`. Across all routers: `threads.py`, `calendar.py`, `cache.py`, `workspaces.py`, `market_data.py`, `mcp_servers.py`, `oauth.py`, `portfolio.py`, `watchlist.py`.
Severity: High | Effort: Low | Impact: High
This exposes internal state (database connection strings, file paths, stack internals) of a financial application to unauthenticated or malicious clients.

**Finding: `calculate_next_thread_index` silently returns 0 on database error**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/database/conversation.py` lines 289-291 — `except Exception as e: logger.error(...); return 0`. If the MAX query fails, thread creation proceeds with thread_index=0, potentially causing unique constraint violations caught downstream by a retry loop, masking the real database problem.
Severity: High | Effort: Low | Impact: High

**Finding: No unit tests for `_handle_send_message` — the 350-line critical chat hot path**
Evidence: `tests/unit/server/app/` has `test_threads_provenance.py`, `test_threads_credit_gate.py`, `test_threads_malformed_id.py`, `test_threads_content_location.py` but no test for `_handle_send_message` (lines 441-781) which contains LLM config resolution, credit checks, agent mode routing, background dispatch, and SSE streaming setup.
Severity: High | Effort: High | Impact: High

**Finding: Credit/usage functions embedded in the 2,734-line conversation.py monolith**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/database/conversation.py` lines 2118-2470 — `create_usage_record`, `update_usage_record`, `get_user_total_credits`, `get_user_credit_history`, `get_response_usage`, `get_thread_credits`, `get_workspace_credits` all live alongside 47 other functions. For a financial application, billing/credit operations warrant isolation for auditability.
Severity: Medium | Effort: Medium | Impact: High

**Finding: Duplicated PTC-dispatch vs flash-dispatch admission logic in threads.py**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/app/threads.py` lines 620-675 (flash) and 707-768 (PTC) contain nearly identical admission-lock, `wait_for_admission`, `pre_register`, and `_track_task(asyncio.create_task(...))` sequences.
Severity: Medium | Effort: Medium | Impact: Medium

**Finding: Duplicated `if conn: ... else: async with get_db_connection()` branching in 15 database functions**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/database/conversation.py` — 15 occurrences of the optional `conn` parameter pattern (e.g., `calculate_next_thread_index` lines 262-287, `create_thread` lines 352-366).
Severity: Medium | Effort: Medium | Impact: Medium

**Finding: Module globals as dependency injection container in setup.py**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/app/setup.py` lines 66-72 — 7 module-level `None` globals assigned via `global` keyword inside `lifespan()`, imported across 48+ call sites.
Severity: Medium | Effort: High | Impact: Medium

**Finding: Temporary diagnostic middleware left in production**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/app/setup.py` lines 629-688 — `MalformedIdDiagnosticMiddleware` with explicit `TEMP (malformed-id-diag)` tags and removal instructions.
Severity: Medium | Effort: Low | Impact: Low

**Finding: INTERNAL_SERVER_ERROR_DETAIL constant defined in 3 files, never used**
Evidence: `src/server/app/setup.py:63`, `src/server/app/cache.py:18`, `src/server/app/utilities.py:20`.
Severity: Low | Effort: Low | Impact: Low

### Feature Gaps & Enhancement Opportunities

**Existing Features (strong foundation):**
- AI chat with PTC sandbox execution, Flash mode, SSE streaming with reconnect
- 5 subagents (research, general-purpose, data-prep, equity-analyst, report-builder)
- 4 financial MCP servers + Yahoo Finance variants + X API
- 25 agent skills (DCF, 3-statement, initiating coverage, comps, earnings, sector, DOCX/XLSX/PPTX/PDF)
- Configurable dashboard with 15+ native + 16 TradingView widgets
- Portfolio and watchlist CRUD with full REST API
- Automated market insights (pre-market, hourly, post-market)
- Automations with cron + price triggers and webhook delivery
- MarketView with real-time WebSocket and agent annotation overlays
- Shared chat links, memo/file uploads, long-term memory
- Full OTel observability (40+ metrics, traces, histograms)
- Onboarding flow, settings with model/key management, BYOK support
- 6 CI workflows

**Finding: No notification/alert delivery system beyond webhooks**
Evidence: Only `WebhookClient` at `src/server/services/webhook_client.py`. No email, SMS, push, or in-app notification center. Automation results and market insights have no way to reach users outside the app.
Severity: High | Effort: Medium | Impact: High

**Finding: No automated report scheduling with email delivery**
Evidence: `src/server/services/automation_executor.py` runs agent workflows and stores results in threads but has no mechanism for recurring PDF reports delivered via email.
Severity: High | Effort: Medium | Impact: High

**Finding: No portfolio P&L tracking or performance visualization**
Evidence: `src/server/app/portfolio.py` is CRUD-only — stores holdings with quantity/cost but has no endpoints for historical performance, unrealized P&L, or return calculations.
Severity: High | Effort: High | Impact: High

**Finding: No admin dashboard or admin API endpoints**
Evidence: No admin-prefixed routes, no admin pages, no admin role checks anywhere in the codebase. No way to view system-wide usage, manage users, or inspect sandbox state from a web UI.
Severity: High | Effort: High | Impact: High

**Finding: No Grafana/Prometheus dashboards despite comprehensive OTel instrumentation**
Evidence: `src/observability/` has 40+ metrics exporting via OTLP, but no dashboard JSON, alerting rules, or runbooks.
Severity: Medium | Effort: Medium | Impact: High

**Finding: No Slack/Discord delivery integration for automations**
Evidence: Automation results only deliverable via webhook or in-app viewing. No native messaging platform connectors.
Severity: Medium | Effort: Medium | Impact: High

**Finding: No conversation search or global search across threads**
Evidence: Thread listing exists but no search endpoint for finding conversations by content, keyword, or date range.
Severity: Medium | Effort: Medium | Impact: Medium

**Finding: Watchlist `alert_settings` are stored but never acted upon**
Evidence: `WatchlistItemCreate` in `src/server/app/watchlist.py` accepts `alert_settings` but no service monitors or fires alerts.
Severity: Medium | Effort: Medium | Impact: Medium

**Finding: No keyboard shortcuts for power-user navigation**
Evidence: Only 4 files reference keyboard/shortcut/hotkey. No global shortcut system (Cmd+K, Cmd+N, etc.).
Severity: Medium | Effort: Medium | Impact: Medium

**Finding: E2E tests cover only 3/7+ page routes**
Evidence: `web/e2e/` contains `chat.spec.js`, `dashboard.spec.js`, `shared-chat.spec.js`. Missing coverage for MarketView, Settings, Automations, Onboarding.
Severity: Medium | Effort: Medium | Impact: Medium

**Finding: No broker integration for live positions or trade execution**
Evidence: Portfolio is tracking-only with manual entry. No Interactive Brokers, Alpaca, or brokerage API integration.
Severity: Medium | Effort: High | Impact: High

**Finding: Only 2 locales supported (en-US, zh-CN)**
Evidence: `web/src/locales/` contains only `en-US.json` and `zh-CN.json`.
Severity: Low | Effort: Medium | Impact: Medium

### Security & Compliance

**Strengths:**
- JWT verification with JWKS, asymmetric RS256/ES256, audience validation
- Consistent ownership checks (`require_workspace_owner`, `require_thread_owner`) on all resource endpoints
- SQL injection protection: parameterized queries throughout, plus NUL-byte sanitization
- Pydantic models enforce `max_length` on most string fields
- API keys and OAuth tokens encrypted at rest using pgcrypto (`pgp_sym_encrypt/pgp_sym_decrypt`)
- Leak detection middleware redacts secrets from agent tool outputs
- Path traversal protection on public file sharing endpoints
- Data provenance with SHA-256 fingerprinting and source attribution
- Error sanitization for API key test responses

**Finding: CORS wildcard `*` with `allow_credentials=True` enables cross-origin account takeover**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/config.yaml` lines 49-50 — `allowed_origins: ["*"]`. `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/app/setup.py` lines 702-715. The CORSMiddleware reflects any Origin when credentials are enabled, allowing any website to make credentialed requests.
Severity: Critical | Effort: Low | Impact: High

**Finding: Default encryption key hardcoded in docker-compose**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/docker-compose.yml` line 61 — `BYOK_ENCRYPTION_KEY: ${BYOK_ENCRYPTION_KEY:-langalpha-local-dev-encryption-key}`. If unset, all API keys and OAuth tokens are encrypted with a publicly known key.
Severity: Critical | Effort: Low | Impact: High

**Finding: Default database credentials hardcoded**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/docker-compose.yml` lines 19-20, line 35 — postgres/postgres and redis defaults. `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/database/conversation.py` lines 90-94 — connection string defaults.
Severity: High | Effort: Low | Impact: High

**Finding: SSL disabled for non-Supabase database connections**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/database/conversation.py` line 97 — `sslmode = "require" if "supabase.com" in db_host else "disable"`.
Severity: High | Effort: Low | Impact: High

**Finding: Docker socket mounted into backend container**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/docker-compose.yml` line 76 — `- ${DOCKER_HOST_SOCKET:-/var/run/docker.sock}:/var/run/docker.sock`. Root-equivalent host access when `SANDBOX_PROVIDER=docker`.
Severity: High | Effort: Medium | Impact: High

**Finding: OAuth email (PII) logged at info level**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/app/oauth.py` line 162 — `logger.info(f"[oauth] Codex connected for user_id={user_id} email={claims.get('email')} plan={claims.get('plan_type')}")`.
Severity: High | Effort: Low | Impact: Medium

**Finding: Thread hard-delete with CASCADE destroys all financial conversation records permanently**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/app/threads.py` lines 328-363 — `delete_thread_endpoint` performs hard DELETE. All queries, responses, SSE events, feedback, and usage records are irrecoverably destroyed.
Severity: High | Effort: Medium | Impact: High

**Finding: No formal audit trail for financial actions**
Evidence: Portfolio CRUD, watchlist changes, and thread deletions are logged at info level to application logs only. No dedicated `audit_log` table, no immutable append-only record.
Severity: High | Effort: High | Impact: High

**Finding: No data retention policy**
Evidence: No TTL, archival, or scheduled cleanup for conversation data. No self-service account deletion endpoint. No configurable retention window.
Severity: High | Effort: High | Impact: High

**Finding: PII stored in plaintext in users table**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/migrations/versions/001_initial_schema.py` lines 77-92 — `email VARCHAR(255)`, `name VARCHAR(255)`, `avatar_url TEXT` stored unencrypted.
Severity: High | Effort: High | Impact: High

**Finding: Portfolio financial positions stored unencrypted**
Evidence: `migrations/versions/001_initial_schema.py` lines 209-230 — `quantity DECIMAL(18,8)`, `average_cost DECIMAL(18,4)`, `account_name VARCHAR(100)` in plaintext. A database breach exposes complete financial profiles.
Severity: High | Effort: High | Impact: High

**Finding: Single symmetric encryption key for all users**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/database/encryption.py` — one `BYOK_ENCRYPTION_KEY` for all pgp_sym_encrypt operations.
Severity: High | Effort: High | Impact: High

**Finding: No rate limiting on most API endpoints**
Evidence: Rate limiting only exists on chat messages (burst guard via Redis INCR) and API key testing (10s cooldown). All other endpoints — user CRUD, portfolio, watchlist, workspace, file access — have no rate limiting.
Severity: Medium | Effort: Medium | Impact: Medium

**Finding: Sessions endpoint lacks authentication**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/app/sessions.py` lines 14-15 — `@router.get("")` has no `CurrentUserId` dependency.
Severity: Medium | Effort: Low | Impact: Medium

**Finding: ChatRequest `messages` content field has no max_length constraint**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/models/chat.py` lines 186-194 — `content: Union[str, List[ContentItem]]` with no size limit.
Severity: Medium | Effort: Low | Impact: Medium

### Performance & Scalability

**Strengths:**
- All database access uses async cursors and `async def` handlers — no blocking I/O on the critical path
- Thread listing endpoints implement server-side pagination (LIMIT/OFFSET, capped at 100)
- Thread/workspace existence checks cached in Redis (24h TTL) for the chat streaming hot path
- `MemoryContextMiddleware` applies 2-second timeout on store reads and fetches user/workspace memory in parallel via `asyncio.gather`
- Compaction middleware wrapped in `asyncio.wait_for(timeout=180s)` preventing hung summarization
- `get_db_connection()` context manager detects non-IDLE connection states and performs rollback/cancel before pool return
- Redis pub/sub properly unsubscribes in `finally` blocks with 30-minute auto-close for abandoned tabs
- Background tasks tracked via `_background_tasks` set with done callbacks preventing GC and unbounded growth
- `WorkspaceManager` compaction guard has safety net in `finally` for abnormal stream endings
- MCP tool discovery cached for 5 minutes with lazy loading

**Finding: Single uvicorn worker — all traffic on one event loop**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/server.py` lines 62-71 — `uvicorn.run()` with no `workers` argument (defaults to 1). CPU-bound middleware (token counting via tiktoken, deep copy, JSON serialization) during one user's chat turn stalls all others. At ~50 concurrent sessions, request latency degrades sharply.
Severity: Critical | Effort: Low (add `workers` arg) / Medium (singleton refactor) | Impact: High

**Finding: In-process singleton architecture prevents horizontal scaling**
Evidence: `WorkspaceManager`, `BackgroundTaskManager`, `SessionService`, `WorkflowTracker` all use `_instance` singletons. Adding `--workers N` creates N independent copies with no coordination. Background tasks, admission locks, and session caches become per-worker islands.
Severity: Critical | Effort: High | Impact: High

**Finding: PostgreSQL connection pool exhaustion risk**
Evidence: Pool defaults: 50 for conversations, 25 for checkpoints = 75 per worker. PostgreSQL default `max_connections=100`. Two workers = 150 connections requested > 100 available. Connection acquisition timeout of 10s means requests queue then fail.
Severity: High | Effort: Medium | Impact: High

**Finding: `get_user_stats()` performs unbounded 4-table LEFT JOIN aggregation**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/database/conversation.py` lines 1951-2023 — joins workspaces → threads → queries → responses → usages with no WHERE date filter. Grows linearly with user history.
Severity: High | Effort: Medium | Impact: High

**Finding: SSE connection fan-out scales linearly with concurrent users**
Evidence: Each active chat holds an SSE connection, plus watch, subagent tasks, and replay endpoints. 2-4 long-lived connections per user. Single worker's FD limit (typically 1024) becomes the ceiling at ~250-500 concurrent users.
Severity: High | Effort: Medium | Impact: High

**Finding: MCP subprocess explosion with concurrent workspaces**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/agent_config.yaml` — 10 enabled MCP servers, each stdio subprocess. Per-workspace subprocess count = workspaces × enabled_servers.
Severity: High | Effort: High | Impact: High

**Finding: Redis connection pool (150 max) under SSE streaming pressure**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/config.yaml` line 107. Code at `src/utils/cache/redis_cache.py` lines 154-176 already has `MaxConnectionsError` logging and pool stats, indicating this has been a production concern.
Severity: Medium | Effort: Low | Impact: High

**Finding: SSE event accumulation uses `copy.deepcopy()` on every event**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/src/server/handlers/streaming_handler.py` lines 196, 201. For long research sessions with thousands of events, this repeated deep copy adds significant CPU overhead.
Severity: Medium | Effort: Medium | Impact: Medium

**Finding: Large JSONB `sse_events` blobs fetched unnecessarily**
Evidence: `get_responses_for_thread()` at `src/server/database/conversation.py` lines 1486-1538 selects `sse_events` for all responses. `get_replay_thread_data` uses `SELECT *`. These multi-MB blobs are pulled even when not needed.
Severity: Medium | Effort: Medium | Impact: High

**Finding: Frontend API client has no timeout or retry configuration**
Evidence: `/home/kent_benson/LangAlpha_financial/LangAlpha/web/src/api/client.ts` lines 28-31 — `axios.create()` with no `timeout` property. Long-running or hung requests never time out client-side.
Severity: Medium | Effort: Low | Impact: Medium

## Recommended Next Session Prompt

```
Fix the three Tier 1 critical issues in LangAlpha:

1. CORS: In config.yaml, replace `allowed_origins: ["*"]` with the actual
   frontend origin(s). Verify CORSMiddleware in src/server/app/setup.py
   only allows listed origins with credentials.

2. Encryption key: In docker-compose.yml, remove the default fallback from
   BYOK_ENCRYPTION_KEY (make it required). Add a startup check in
   src/server/app/setup.py lifespan() that refuses to start if the key
   matches the known default "langalpha-local-dev-encryption-key".

3. Uvicorn workers: In server.py, add a `--workers` argument defaulting to
   `min(4, os.cpu_count())`. Note: the singleton services
   (WorkspaceManager, BackgroundTaskManager, SessionService) need their
   state moved to Redis or a shared backend before multiple workers will
   function correctly — for now, document that workers=1 is the safe
   default and create a tracking issue for the singleton refactor.

Then fix the quick-win Tier 2 items:
4. Replace all `str(e)` in HTTP error responses with generic messages
   (grep -rn 'str(e)' src/server/app/).
5. Fix calculate_next_thread_index to propagate errors instead of
   returning 0.
6. Remove default DB password fallbacks from docker-compose.yml and
   connection code.
7. Change sslmode logic to default to "require" instead of "disable".
8. Remove email from oauth.py info-level log message.
```

## Report Metadata
| Field          | Value                                                    |
|----------------|----------------------------------------------------------|
| Generated      | 2026-07-01 09:43 UTC                                    |
| Project Root   | /home/kent_benson/LangAlpha_financial/LangAlpha          |
| Files Reviewed | ~190 (across 4 parallel evaluation agents)               |
| Total Findings | 88                                                       |
| Skill Version  | evaluate-app v1.0                                        |
